import { BlurOnClickOutsideDirective } from './blur-on-click-outside.directive';

describe('BlurOnClickOutsideDirective', () => {
  it('should create an instance', () => {
    const directive = new BlurOnClickOutsideDirective();
    expect(directive).toBeTruthy();
  });
});

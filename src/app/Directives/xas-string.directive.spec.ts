import { XasStringDirective } from './xas-string.directive';

describe('XasStringDirective', () => {
  it('should create an instance', () => {
    const directive = new XasStringDirective();
    expect(directive).toBeTruthy();
  });
});

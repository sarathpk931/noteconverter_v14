import { NgScrollableDirective } from './ng-scrollable.directive';

describe('NgScrollableDirective', () => {
  it('should create an instance', () => {
    const directive = new NgScrollableDirective();
    expect(directive).toBeTruthy();
  });
});

import { TextFieldDirective } from './text-field.directive';

describe('TextFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new TextFieldDirective();
    expect(directive).toBeTruthy();
  });
});

import { ActionBarDirective } from './action-bar.directive';

describe('ActionBarDirective', () => {
  it('should create an instance', () => {
    const directive = new ActionBarDirective();
    expect(directive).toBeTruthy();
  });
});

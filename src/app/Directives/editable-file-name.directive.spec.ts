import { EditableFileNameDirective } from './editable-file-name.directive';

describe('EditableFileNameDirective', () => {
  it('should create an instance', () => {
    const directive = new EditableFileNameDirective();
    expect(directive).toBeTruthy();
  });
});

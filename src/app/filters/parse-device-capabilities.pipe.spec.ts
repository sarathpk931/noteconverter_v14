import { ParseDeviceCapabilitiesPipe } from './parse-device-capabilities.pipe';

describe('ParseDeviceCapabilitiesPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseDeviceCapabilitiesPipe();
    expect(pipe).toBeTruthy();
  });
});

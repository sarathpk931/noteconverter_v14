import { ConvertProtocolPipe } from './convert-protocol.pipe';

describe('ConvertProtocolPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertProtocolPipe();
    expect(pipe).toBeTruthy();
  });
});

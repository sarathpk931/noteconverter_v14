import { OrderObjectByPipe } from './order-object-by.pipe';

describe('OrderObjectByPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderObjectByPipe();
    expect(pipe).toBeTruthy();
  });
});
